# Wi-Fi and UART state

Unknown because Linux has not been reached through an approved SSH session. No Wi-Fi file, `.env`, token, private key, or board UART device was read. The only host serial port opened was `/dev/ttyACM0`, read-only; no data was written. Board UART ownership and Wi-Fi state await the future read-only audit.
