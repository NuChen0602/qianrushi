# Actual boot.cfg state

Unknown. No SSH session was established, so `/boot/boot.cfg`, its hashes/backups, default kernel, title blocks, and `showmenu` value were not read. No boot configuration change was attempted. If later audit shows `timeout 3`, `default 0`, `showmenu 0`, it remains pending manual cold-start validation and must not be modified.
