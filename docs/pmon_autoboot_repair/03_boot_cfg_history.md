# boot.cfg history

No board-side file was read, backed up, or changed. Thus no current configuration, hash, history, or difference can be reported. The sole permitted future trial is implemented but not invoked: it only accepts `timeout 3`, `default 0`, `showmenu 1`, normal `(emmc0,0)/boot/vmlinuz`, and no `console=ttyS0` in the first menu entry.
